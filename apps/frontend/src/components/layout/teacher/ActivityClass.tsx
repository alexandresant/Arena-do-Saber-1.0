import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function ActivtyClass(){

    return(
        <Card>
            <CardHeader>
                <CardTitle>Atividades nesta turma</CardTitle>
                <CardDescription>Turma {}</CardDescription>
            </CardHeader>
            <CardContent>
                
            </CardContent>
        </Card>
    )
}